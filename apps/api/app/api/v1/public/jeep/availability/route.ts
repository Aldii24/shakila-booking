import { assertJeepDepartureOpen,calculateJeepAvailability,getJeepProduct } from "@booking/booking"; import { jeepAvailabilityRequestSchema } from "@booking/contracts"; import { body,failure,ok } from "@/lib/http";
export async function POST(request:Request){try{const input=jeepAvailabilityRequestSchema.parse(await body(request));const product=await getJeepProduct(input.packageSlug,input.departureSlotId);assertJeepDepartureOpen(input.tourDate,product.slot.departureTime,"Asia/Jakarta");const availableQuantity=await calculateJeepAvailability({businessId:product.package.businessId,tourDate:input.tourDate,departureSlotId:product.slot.id});return ok({packageSlug:input.packageSlug,departureSlotId:input.departureSlotId,tourDate:input.tourDate,availableQuantity,capacityPerUnit:product.package.capacityPerUnit});}catch(e){return failure(e);}}
