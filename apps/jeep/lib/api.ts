export const API_URL=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3003/api/v1";export const rupiah=(n:number)=>`Rp${new Intl.NumberFormat("id-ID").format(n)}`;
export async function api<T>(path:string,init?:RequestInit):Promise<T>{const r=await fetch(`${API_URL}${path}`,{...init,headers:{"Content-Type":"application/json",...init?.headers},cache:"no-store"});const e=await r.json() as {data:T|null;error:{code:string}|null};if(!r.ok||e.error)throw new Error(e.error?.code??"NETWORK_ERROR");return e.data as T}
export const message=(code:string)=>({INVENTORY_NOT_AVAILABLE:"Jeep terakhir baru saja dipesan. Pilih slot atau jumlah lain.",INVALID_GUEST_COUNT:"Jumlah tamu melebihi kapasitas Jeep.",DEPARTURE_SLOT_CLOSED:"Jam keberangkatan tersebut sudah lewat menurut waktu lokal WIB. Pilih jam berikutnya atau tanggal lain.",BOOKING_LOOKUP_FAILED:"Kode booking dan kontak tidak cocok.",INVALID_PAYMENT_PROOF:"Bukti harus berupa JPG, JPEG, atau PNG dengan ukuran maksimal 5 MB.",BOOKING_EXPIRED:"Batas pembayaran booking telah berakhir.",HUMAN_VERIFICATION_REQUIRED:"Turnstile belum dikonfigurasi pada lingkungan ini.",NETWORK_ERROR:"Koneksi tidak stabil. Coba kembali."} as Record<string,string>)[code]??"Permintaan belum dapat diproses.";
