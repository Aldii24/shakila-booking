export const API_URL=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3003/api/v1";
export const rupiah=(value:number)=>`Rp${new Intl.NumberFormat("id-ID").format(value)}`;
export async function api<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(`${API_URL}${path}`,{...init,headers:{"Content-Type":"application/json",...init?.headers},cache:"no-store"});const envelope=await response.json() as {data:T|null;error:{code:string;message:string}|null};if(!response.ok||envelope.error)throw new Error(envelope.error?.code??"NETWORK_ERROR");return envelope.data as T;}
export function message(code:string){return ({INVENTORY_NOT_AVAILABLE:"Ketersediaan baru saja berubah. Silakan pilih tanggal atau jumlah lain.",INVALID_GUEST_COUNT:"Jumlah tamu melebihi kapasitas unit yang dipilih.",HUMAN_VERIFICATION_REQUIRED:"Verifikasi keamanan belum dikonfigurasi.",HUMAN_VERIFICATION_FAILED:"Verifikasi keamanan gagal. Silakan coba lagi.",BOOKING_LOOKUP_FAILED:"Kode booking dan kontak tidak cocok.",INVALID_PAYMENT_PROOF:"Bukti harus berupa JPG, JPEG, atau PNG dengan ukuran maksimal 5 MB.",BOOKING_EXPIRED:"Batas pembayaran booking telah berakhir.",NETWORK_ERROR:"Koneksi terputus. Silakan coba lagi."} as Record<string,string>)[code]??"Permintaan belum dapat diproses. Silakan coba lagi."}
