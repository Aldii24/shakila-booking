import { getBookingStatus,verifyBookingAccessToken } from "@booking/booking"; import { accessSecret,bearer,failure,ok } from "@/lib/http";
export async function GET(request:Request,{params}:{params:Promise<{bookingCode:string}>}){try{const {bookingCode}=await params;const payload=verifyBookingAccessToken(bearer(request),accessSecret(),bookingCode,"booking:read");return ok(await getBookingStatus(bookingCode,payload.bookingId));}catch(e){return failure(e);}}
