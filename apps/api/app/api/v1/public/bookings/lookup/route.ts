import { bookingLookupRequestSchema } from "@booking/contracts"; import { issueBookingAccessToken,lookupBooking } from "@booking/booking"; import { accessSecret,body,failure,ok } from "@/lib/http"; import { verifyHuman } from "@/lib/human-verification";
export async function POST(request:Request){try{const input=bookingLookupRequestSchema.parse(await body(request));await verifyHuman(input.turnstileToken,request,"lookup");const result=await lookupBooking(input);const accessToken=issueBookingAccessToken({bookingId:result.bookingId,bookingCode:result.bookingCode,scopes:["booking:read","payment:create","invoice:read"]},accessSecret());return ok({...result,accessToken});}catch(e){return failure(e);}}
