import {BookingForm} from "@/components/booking-flow";import {Footer,Header} from "@/components/site-chrome";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <><Header/><main className="page-shell"><div className="page-intro"><p className="kicker">PASSENGER MANIFEST</p><h1>LOCK THE FLEET.</h1><p>Server akan menghitung harga dan memeriksa ulang Jeep tepat saat booking dibuat.</p></div><BookingForm selection={{packageSlug:q.packageSlug??"",tourDate:q.tourDate??"",departureSlotId:q.departureSlotId??"",guestCount:q.guestCount??"4",quantity:q.quantity??"1"}}/></main><Footer/></>}
