import { createBooking,issueBookingAccessToken } from "@booking/booking"; import { createBookingRequestSchema } from "@booking/contracts"; import { z } from "zod"; import { body,failure,ok,accessSecret } from "@/lib/http"; import { verifyHuman } from "@/lib/human-verification";
export async function POST(request:Request){try{const input=createBookingRequestSchema.parse(await body(request));await verifyHuman(input.turnstileToken,request);const key=z.uuid().parse(request.headers.get("idempotency-key"));const result=await createBooking(input,key);const accessToken=issueBookingAccessToken({bookingId:result.bookingId,bookingCode:result.bookingCode,scopes:["booking:read","payment:create","invoice:read"]},accessSecret());return ok({...result,accessToken},201);}catch(e){return failure(e);}}
