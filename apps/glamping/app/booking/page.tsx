import {BookingForm} from "@/components/booking-flow";import {Footer,Header} from "@/components/site-chrome";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <><Header/><main className="page-shell"><div className="page-intro"><p className="eyebrow">Detail tamu</p><h1>Hampir sampai.</h1><p>Lengkapi detail Anda. Harga dan DP dihitung ulang oleh server sebelum reservasi dibuat.</p></div><BookingForm selection={{productSlug:q.productSlug??"",checkInDate:q.checkInDate??"",checkOutDate:q.checkOutDate??"",guestCount:q.guestCount??"2",quantity:q.quantity??"1"}}/></main><Footer/></>}
